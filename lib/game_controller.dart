class GameController {
  // الفيز 1: المصفوفة التي تمثل المربعات
  // رقم 0 يمثل المكان الفارغ في اللعبة
  List<int> board = [1, 2, 3, 4, 5, 6, 7, 8, 0];

  // الفيز 2: دالة خلط الأرقام (Shuffle)
  // تستدعيها عند بدء اللعبة أو عند الضغط على زر إعادة التشغيل
  void shuffleBoard() {
    board.shuffle();
  }
}